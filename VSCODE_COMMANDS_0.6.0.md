# VS Code commands
```powershell
cd C:\FibroChat
npm config set registry https://registry.npmjs.org/
npm install
npm run check
npm run smoke
git status
git add .
git commit -m "FibroChat v0.6.0 - Administration and Security"
git push origin main
```
Then Force Rebuild & Redeploy in Coolify.
