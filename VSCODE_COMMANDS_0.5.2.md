# Команды для Visual Studio Code

```bash
npm ci
npm run check
npm run smoke
git status
git add .
git commit -m "FibroChat v0.5.2: profile and privacy"
git push origin main
```

После push откройте Coolify и выполните **Redeploy**. При сомнениях в кэше используйте **Force Rebuild**.
