{
  "name": "fibrochat",
  "version": "0.4.1",
  "private": true,
  "description": "FibroChat PostgreSQL Core",
  "scripts": {
    "start": "node server/server.js",
    "check": "node --check server/server.js && node --check server/application.js && node --check server/storage/store.js && node --check client/app.js",
    "restore-network": "node tools/restore-network-backup.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
