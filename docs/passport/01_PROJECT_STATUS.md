# Project Status

Implemented: web client, Head Node, encrypted message envelopes, signatures, invites, user approval, roles, subscriptions, support, trusted devices, sessions, Key Vault, realtime SSE events, delivery retry queue, network profile, protocol metadata, Coolify deployment, PostgreSQL persistence, password change.

Current deployment: one VPS with Docker and Coolify. PostgreSQL runs as a private Coolify database.

Next verified work: complete browser QR rendering/scanning, domain and HTTPS, production hardening, then second VPS replication/failover.
